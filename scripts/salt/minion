file_client: local

# default is warn
log_level: info

fileserver_backend:
    - roots
    - git

gitfs_provider: GitPython
gitfs_remotes:
    # all minions will have this available to them
    - ssh://git@github.com/elifesciences/elife-base-formula.git

file_roots:
    base:
        - /srv/salt/

git_pillar_provider: GitPython
ext_pillar:
    - git: 
        - master ssh://git@github.com/elifesciences/elife-base-formula.git:
            # possibly redundant
            - root: pillar
            - env: base

pillar_roots:
    base:
        - /srv/pillar

extension_modules: 
    /srv/salt/modules/
