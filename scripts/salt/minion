file_client: local

# default is warn
log_level: info

fileserver_backend:
    - roots
    - git

gitfs_provider: GitPython

# desirable remotely
#gitfs_remotes:
    # this is possible, but doesn't use modified files, defeating purpose
    #- file:///vagrant/cloned-projects/builder-base-formula
#    - ssh://git@github.com/elifesciences/builder-base-formula.git

file_roots:
    base:
        - /vagrant/cloned-projects/builder-base-formula
        - /srv/salt/

git_pillar_provider: GitPython
# desirable remotely
#ext_pillar:
#    - git: 
#        - master ssh://git@github.com/elifesciences/builder-base-formula.git:
#            # possibly redundant
#            - root: pillar
#            - env: base

pillar_roots:
    base:
        - /vagrant/cloned-projects/builder-base-formula/pillar/
        - /srv/pillar

extension_modules: 
    /srv/salt/modules/
