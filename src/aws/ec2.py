def restart(stackname, node):
    """You probably want 'lifecycle.restart'.
    Restarts an ec2 instance, polling until available.
    Good for kicking individual stuck instances without affecting all
    nodes in cluster."""
    #
    pass
